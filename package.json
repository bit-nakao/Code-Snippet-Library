{
  "name": "code-snippet-library-root",
  "version": "1.0.0",
  "description": "Root package for Code Snippet Library",
  "scripts": {
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "dev": "npx concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm install && npm run build && cd .. && npx rimraf dist && npx cpx \"client/dist/**/*\" dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cpx": "^1.5.0",
    "rimraf": "^6.1.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.100.0",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "multer": "^2.1.1"
  }
}